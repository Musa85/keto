# 📘 SPEC — كيتو: دليلي الشامل

> وثيقة مرجعية كاملة للمشروع. أعطِ هذا الملف لأي AI (أو اقرأه في محادثة جديدة) ليفهم كل شيء قبل أي تعديل أو إضافة.
> آخر تحديث: 2026-06-25

---

## 1. ما هو المشروع؟

تطبيق ويب **بملف HTML واحد** (`index.html`) عربي كامل (RTL)، يعمل أوفلاين بدون سيرفر، يجمع كل ما يخص رحلة موسى مع حمية الكيتو: المراحل، الوصفات (تجميد + مباشر)، المكمّلات، السناكات، وخطة تحضير قابلة للطباعة الاحترافية.

- **المستودع**: https://github.com/Musa85/keto (الحساب: `Musa85`)
- **المنشور**: https://musa85.github.io/keto/ (GitHub Pages، فرع main، المسار `/`)
- **المسار المحلي**: `/home/musa/projects/keto`
- **التقنية**: HTML + CSS + Vanilla JS فقط. لا frameworks، لا dependencies. خطوط Google فقط (Reem Kufi للعناوين، Cairo للنص، JetBrains Mono للأرقام).
- **التخزين**: `localStorage` بمفتاح `keto_hub_v1`.

## 2. سياق المستخدم (مهم جداً لأي محتوى جديد)

- موسى، سعودي، يتبع الكيتو على 3 مراحل: **صارم** (دخول الكيتوزس، كارب ≤20جم، أسبوعان–3) ← **عادي** (إنقاص وزن، ≤30جم) ← **لايف ستايل** (تثبيت، ≤50جم).
- **يأخذ وجبات مجمّدة للعمل** ويسخّنها بمايكروويف المكتب ⇒ تصنيف **الرائحة** (`odor: low|med`) أساسي حتى لا يزعج الزملاء. `low` = آمنة للمكتب.
- **لا يحب الدجاج المشوي الجاف** — الطرق المفضلة: مقلاة بالزبدة، صلصات كريمية، فرن بحشوات، مفتفت بصلصة.
- **البروتين البحري**: فيليه سمك أبيض مجمّد + **روبيان صغير مجمّد مقشّر** فقط (لا سلمون ولا تونة طازجة؛ تونة معلبة مقبولة).
- **بروتين بودر**: آيزوليت واي «ISOTOPE فانيلا» + بيف بروتين «BEEF-XP بلاك كرنت» — قسم كامل لوصفاتهما.
- المكوّنات سعودية متوفرة محلياً. الأسبوع يبدأ السبت. الخطاب بصيغة المذكّر.
- التحلية: ستيفيا/إريثريتول فقط. ممنوع السكر والعسل والمالتيتول.
- **⚠️ جوزة الطيب ممنوعة قانونياً في السعودية — لا تُذكر في أي وصفة أبداً.** البدائل: فلفل أبيض (للصلصات البيضاء)، هيل/زنجبيل مطحون (للحلى).

## 3. بنية الملفات والبناء

```
keto/
├── index.html      ← الناتج النهائي الوحيد المنشور (~250KB)
├── README.md
├── SPEC.md         ← هذا الملف
├── .gitignore      (يستثني build/)
└── build/          ← أجزاء المصدر، يُجمَّع منها index.html
    ├── 00_head.html        (head + كل CSS + هيكل body حتى وسم <script>)
    ├── 01_freezer_data.js  (ING, PHASES, BASES, EQUIP, AVAIL, SNACKS, RECIPES — من keto_master.html الأصلي)
    ├── 02_guide_data.js    (GUIDE_PHASES, GUIDE_CATS, GSMAP, GUIDE_DATA, SWAPS)
    ├── 03_fresh_data.js    (SECTIONS, SALADS — وصفات الطبخ المباشر)
    ├── 04_protein_data.js  (TYPES, PH, PSNACKS, FAST, BASE_LABEL — سناكات البودر)
    ├── 05_shop_data.js     (SHOP_CATS, SHOP_TOOLS — تسوق ركن البروتين)
    ├── 06_extra_data.js    (كل الإضافات الجديدة: مكوّنات، وصفات، روبيان، مايكروويف، مكمّلات، محتوى الرحلة)
    └── 20_app.js           (كل منطق الواجهة + يُغلق </script></body></html>)
```

**أمر التجميع** (يُنفَّذ بعد أي تعديل على build/):
```bash
cd build && cat 00_head.html 01_freezer_data.js 02_guide_data.js 03_fresh_data.js 04_protein_data.js 05_shop_data.js 06_extra_data.js 20_app.js > ../index.html
```
> يمكن التعديل على index.html مباشرة أيضاً، لكن الأفضل عبر build/ لبقاء المصدر مرتباً.

**فحص الصياغة**: `cat build/0*.js > /tmp/a.js && head -n -3 build/20_app.js >> /tmp/a.js && node --check /tmp/a.js`

**اختبار فعلي** (headless):
```bash
google-chrome --headless --no-sandbox --virtual-time-budget=4000 --dump-dom "file://$PWD/index.html" | grep '<title>'
# ولتوليد PDF الطباعة:
# أضف سكربت يملأ state.plan ويستدعي buildPrintDoc() + body.classList.add("pp") ثم --print-to-pdf
```

## 4. التبويبات السبعة

| id | الاسم | المصدر | ملاحظات |
|----|------|--------|---------|
| `journey` | 🧭 رحلتي والدليل | PHASES, TRANSITIONS, KETO_SIGNS, KETO_FLU, GUIDE_DATA, SWAPS | بطاقات المراحل قابلة للنقر لتغيير المرحلة |
| `freezer` | 🧊 وجبات التجميد | RECIPES (70، m01–m70) | فلاتر: البروتين، مرحلتي فقط، رائحة خفيفة، بدون فرن/فرن/مايكروويف مباشرة |
| `fresh` | 🍳 طبخ مباشر | SECTIONS (7 أقسام، 74 وصفة) | أقسام: بيض، دجاج، لحم، دجاج مفروم، لحم مفروم، سمك، روبيان. **كل وصفة لها `mac` (ماكروز/حصة) + أوزان في `ing`** |
| `sides` | 🥗 مكمّلات الوجبة | SIDE_GROUPS (4 مجموعات، 33) | سلطات (لها `how` بكميات)، جوانب ساخنة، صلصات، أمثلة «كمّل وجبتك» |
| `protein` | 🥤 سناكات البروتين | PSNACKS (37) + SHOP_CATS | فلاتر: واي/بيف، النوع، الفورية |
| `snacks` | 🥜 سناكات خفيفة | SNACKS (50، كائن s01–s50) | بدون بروتين بودر. **كل سناك له `steps` + `store` + `prepTxt` + `cat`**. فلتر بالتصنيف (`SNACK_CATS`: حلو/مالح/مكسرات/خضار/بروتيني) عبر `snCat`/`data-sncat`. منها **24 حلو** |
| `plan` | 🖨️ خطة التحضير | state.plan | انظر §6 |

ميزات عامة: بحث بالقسم النشط (`#q`)، مفضلة ⭐ (`state.favs`)، زر «🛒 + للخطة» على كل بطاقة، اختيار المرحلة يلوّن الواجهة (PHASE_COLORS) ويُفلتر الدليل.

## 5. مخططات البيانات (Schemas)

### وصفة تجميد (RECIPES — مصفوفة):
```js
{ id:'m51', emoji:'⚡', name:'…', equip:'stove|oven|both|micro',
  altMethod:'(اختياري) طريقة بديلة', desc:'سطر وصفي',
  base:'chicken_breast|chicken_ground|beef_ground|white_fish|eggs|shrimp',  // مفاتيح BASES
  diets:['strict','normal','lifestyle'],  // المراحل المناسبة
  odor:'low|med',          // low = آمنة لمايكروويف العمل
  freeze:'ممتاز|جيد|…', reheat:'نص التسخين', store:'نص التخزين',
  prepMin:5, cookMin:8, difficulty:'سهل', kcal:380, p:42, f:22, c:3,  // لحصة واحدة
  snacks:['s04','s17'],    // سناكات مقترحة معها (مفاتيح SNACKS)
  ingredients:[{key:'shrimp', amt:200, alts:['ghee']}],  // key من ING، amt بوحدة ING[key].unit
  steps:['…'] }
```
### مكوّن (ING — كائن): `key: { name:'عربي', cat:'بروتين|ألبان وأجبان|دهون|خضار|مكسرات|بهارات وصلصات', unit:'جم|مل|حبة|فص|علبة|حزمة' }`
> **عند إضافة وصفة بمكوّن جديد: أضف المكوّن لـ ING أولاً** (في 06_extra_data.js عبر `Object.assign(ING, {...})`) وإلا لن يظهر في القوائم.

### سناك خفيف (SNACKS — كائن بمفاتيح s01…): حقول الماكروز + `ingredients` المهيكلة + **`steps:[…]` (طريقة) + `store` (تحضير مسبق) + `prepTxt` (وقت) + `cat` (مفتاح من `SNACK_CATS`: sweet/savory/crunchy/veggie/protein)**. تُعرض الخطوات والتخزين ووسم التصنيف في بطاقة السناك، ويُفلتر بالتصنيف. `SNACK_CATS` معرّف في `06_extra_data.js`، وتصنيفات s01–s35 مُسندة عبر خريطة هناك، والجديدة تحمل `cat` داخلها. عند إضافة سناك جديد: ضع `cat` مناسباً.
### وصفة مباشرة (SECTIONS[i].recipes): `{ name, meta:['١٥ دقيقة','مقلاة'], mac:{serves,kcal,p,f,c}, ing:['نصوص حرة بأوزان'], steps:[…], tip }` — `ing` **نصية لا تُجمَع كمياتها آلياً**، لكن `mac` (لكل حصة) تُعرض كصف ماكروز في البطاقة وخطة التحضير والطباعة. عند إضافة وصفة مباشرة: ضع وزناً في كل سطر `ing` وأضف `mac` بعدد `serves` صادق.
### سناك بروتين (PSNACKS): `{ id:عدد, e, n, base:'whey|beef|both', type:مفتاح TYPES, ph:'strict|normal', desc, P,F,C,kcal, ing:[نصوص], steps, store }`.
### مكمّل (SIDE_GROUPS[i].items): `{ e, n, d, how:'(اختياري) الطريقة' }`.
### دليل المسموح (GUIDE_DATA): `{ cat:مفتاح GUIDE_CATS, e, n, s:{strict:'y|c|n',…}, cond, pc:{strict:'ملاحظة للمرحلة'} }`.

## 6. خطة التحضير والطباعة (أهم ميزة)

- **uid لكل عنصر**: `fz:m01` (تجميد) · `sn:s09` (سناك) · `pr:12` (بروتين) · `fr:القسم:اسم الوصفة` (مباشر) · `sd:اسم المجموعة:الاسم` (مكمّل). الدالة `resolve(uid)` تعيد `{t, o, e, n, lbl, struct}` حيث `struct=1` يعني مكوّنات مهيكلة قابلة للجمع.
- `state.plan = { uid: عدد_الحصص }` و `state.have = []` (المشطوب «متوفر بالمنزل»: مفاتيح `g:ingKey` للمجمّعة و `t:اسم:سطر` للنصية).
- **قائمة التسوق** تُجمَّع آلياً من المكوّنات المهيكلة × الحصص، بترتيب الفئات: بروتين ← ألبان ← دهون ← خضار ← مكسرات ← بهارات، مع تحويل ≥1000جم/مل إلى كجم/لتر (`fmtTotal`). الوصفات النصية تُسرَد تحت «حسب الوصفة».
- **الطباعة**: زر الطباعة يستدعي `printPlan()` التي تبني المستند بـ `buildPrintDoc()` في `#printdoc` (مخفي على الشاشة)، تضيف `body.pp`، ثم `window.print()`. **مهم**: إزالة `body.pp` تتم عند حدث `afterprint` (مع بديل `matchMedia('print')`)، **وليس** فوراً بعد `window.print()` — لأن `window.print()` في كروم/بريف بالجوال غير محظور (يرجع فوراً)، فالحذف الفوري كان يلغي `pp` قبل الالتقاط فيطبع الصفحة الحالية. كتلة `@media print` الأولى (احتياط Ctrl+P) مُقيّدة بـ `body:not(.pp)` حتى لا تُظهر التبويبات أثناء طباعة الخطة. CSS الطباعة ببادئة `pd-`: ترويسة بخط مزدوج، جدول ملخص، بطاقات وصفات مرقمة بعمودين (مكوّنات مضروبة | خطوات)، قائمة تسوق بعمودين بمربعات شطب، تذييل. لون التمييز `--pacc` من لون المرحلة. الحفظ PDF = نفس الطباعة من المتصفح.
- البطاقات بلا خطوات تتمدد بعرض كامل (fallback: steps ← how ← desc).

## 7. الحالة والأحداث

- `state = { phase, tab, favs[], favOnly, plan{}, have[] }` — تُحفظ كاملة عند كل تغيير (`save()`).
- كل التفاعل عبر **مستمع نقر واحد** على `document` بفحص `data-*`: `data-phase, data-tab, data-fav, data-plan, data-pinc/pdec/prem, data-have, data-print, data-clearplan, data-fzbase, data-fztog, data-frcat, data-prbase, data-prtype, data-prfast, data-gstatus` + فتح البطاقة بالنقر على رأسها.
- `RENDER = {tab: دالة}` و `renderActive()` تعيد رسم التبويب النشط فقط.

## 8. التصميم

- داكن دافئ: خلفية `#0F0D0A`، بطاقات `#1C1813`، ألوان المراحل: صارم `#C85A4A`، عادي `#E8A33D`، لايف ستايل `#7FA86B` (في `PHASE_COLORS` بالـ JS و CSS vars `--strict/--normal/--lifestyle`؛ `--acc` يتبدل مع المرحلة).
- mobile-first، شبكة بطاقات `minmax(310px,1fr)`، تبويبات قابلة للتمرير أفقياً.

## 9. بيئة النشر والتطوير (مهم)

- **شبكة المستخدم متقطعة مع github.com** (نقطة 20.233.x.x الخليجية): لُفّ كل `git push / gh api` بحلقة إعادة محاولة (10–30 مرة × sleep 10).
- `gh` مثبت يدوياً في `~/.local/bin/gh` (ليس في PATH — استخدم `export PATH="$HOME/.local/bin:$PATH"`). المصادقة تمت بـ device flow.
- لا sudo. حذف الملفات: hook يفرض `trash` بدل `rm`.
- git identity محلي بالريبو: Musa / mosas85@gmail.com.
- المصادر التاريخية (النسخ القديمة قبل الدمج) في: `/home/musa/projects/20260527-خطة كيتو-فابل/` — أهمها `keto_master.html` (تجميد ودليل) و `keto_pro.html` مع `keto_pro_spec.md` (مخطط أسبوعي تفاعلي قديم بحاسبة ماكروز).

## 10. قواعد إضافة محتوى جديد

1. وصفة تجميد: أضف كائناً لـ RECIPES في `06_extra_data.js` (id تسلسلي `m71+`)، وأي مكوّن جديد إلى ING. حدّد `odor` بصدق و`reheat` عملياً. (المكوّنات الجديدة الأخيرة مُعرّفة في `Object.assign(ING,…)` قرب نهاية الملف: حليب جوز الهند، ملفوف، شطة، محلٍّ، جوز هند مبشور، زبدة فول سوداني، شيا، شوكولاتة داكنة، بذور يقطين، صلصة حارة، قرفة، خل.)
2. وصفة مباشرة: أدرجها في `EXTRA_FRESH`/`EXTRA_FRESH2`/`EXTRA_FRESH3` أو `SECTIONS.find/​push` (مع `e` إيموجي) — **أضف `mac:{serves,kcal,p,f,c}` وأوزاناً في `ing`**.
3. سناك: `Object.assign(SNACKS, { s36: {...} })` — مع `steps` و`store` و`prepTxt`.
4. التزم بالعربية الفصحى الميسّرة، صيغة المذكّر، أرقام مكوّنات واقعية، وماكروز منطقية (سعرات ≈ 4ب + 4ك + 9د تقريباً).
5. بعد أي تعديل: جمّع، `node --check`، اختبر headless، ثم commit + push (بحلقة المحاولات) وتحقق من Pages بعد دقيقة.

## 11. أفكار مؤجلة (Backlog)

- نسخ قائمة التسوق/الخطة نصاً للواتساب.
- PWA (manifest + service worker) ليتثبت كتطبيق جوال ويعمل أوفلاين.
- مشاركة الخطة برابط (ضغط JSON في الـ URL).
- حاسبة ماكروز شخصية (موجودة قديماً في keto_pro.html — يمكن نقلها) + مقارنة الخطة بالأهداف.
- متتبع وزن برسم بياني + عداد صيام حي.
- قوالب خطط جاهزة بضغطة («أسبوع عمل صارم»…).
- ملاحظات/تقييم شخصي على كل وصفة.
- تصدير/استيراد JSON للنسخ الاحتياطي.
- توزيع الخطة على أيام الأسبوع (السبت أولاً).
